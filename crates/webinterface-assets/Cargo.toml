[package]
name = "webinterface-assets"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
webinterface-core = { path = "../webinterface-core" }


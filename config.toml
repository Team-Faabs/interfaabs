[server]
host = "0.0.0.0"
port = 8080

[crashpilot]
# Crashpilot websocket endpoint used for commands and vision/tracked streaming
ws_url = "ws://10.0.64.221:4096/ws"

# Default source: "vision" or "tracked"
default_source = "tracked"

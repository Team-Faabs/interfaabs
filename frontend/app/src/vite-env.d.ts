/// <reference types="vite/client" />

// Brings in Vite's ambient module declarations, including the CSS-module and
// `.scss` ones that `@faabs/schema-renderer` relies on. The package is
// compiled from source as part of this app's build, so its declarations have
// to be in scope here.
